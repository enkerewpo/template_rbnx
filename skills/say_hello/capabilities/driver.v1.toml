# Lifecycle interface every skill MUST declare. rbnx boot skips
# CMD_ACTIVATE on skills (they stay at INACTIVE). The executor
# fires CMD_ACTIVATE just-in-time on the first MCP call.
[contract]
id      = "robonix/skill/say_hello/driver"
version = "1"
kind    = "skill"
idl     = "lifecycle/srv/Driver.srv"

[mode]
type = "rpc"
